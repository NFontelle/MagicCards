package MagicCards;

public enum Couleur {
	ROUGE, VERT, BLEU, BLANC, NOIR

	
	
	/*
int comparerForce(Couleur opposant)
{
 int forceRelative = 0;
 switch (this) {
 case BLEU: 
	 if(opposant == Couleur.ROUGE) {forceRelative = 1;}
	 if(opposant == Couleur.VERT) {forceRelative = -1;}
	 // ici manque si meme couleur, mieux serait de faire enum pour que le cas soit pris en compte
	 
	 
	 
	 
	 
 }






return forceRelative;*/

}